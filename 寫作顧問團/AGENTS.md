# 寫作顧問團專案規則

- 使用繁體中文與使用者溝通，除非使用者要求其他語言。
- 文章工作一律使用 `writing-council` Skill。
- 所有文章成果放在 `文章專案/` 的個別文章資料夾。
- 每次開始或接續工作，先讀該文章的 `00-目前進度.md`。
- 未經使用者明確確認，不得進入下一階段。
- 不覆寫既有稿件版本；局部修改必須建立新版本。
- 不修改鎖定段落；若修改要求與鎖定內容衝突，先請使用者決定。
- `07-修改紀錄.md` 只追加，不刪除既有紀錄。
- 以專案檔案為正式記憶，不只依賴對話紀錄。
- 不使用 SDK 或 API 建立這套工作流。

